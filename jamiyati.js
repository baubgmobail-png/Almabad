const KEY='almaabad_jamiyati_v1';
const money=new Intl.NumberFormat('ar-JO',{maximumFractionDigits:2});const monthFmt=new Intl.DateTimeFormat('ar-JO',{month:'long',year:'numeric'});let items=load(),expanded=null;
function load(){try{return JSON.parse(localStorage.getItem(KEY))||[]}catch{return[]}}function save(){localStorage.setItem(KEY,JSON.stringify(items))}function thisMonth(){const d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}`}function monthAt(start,n){const [y,m]=start.split('-').map(Number),d=new Date(y,m-1+n,1);return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}`}function pretty(v){const [y,m]=v.split('-').map(Number);return monthFmt.format(new Date(y,m-1,1))}function statusOf(x){const n=thisMonth(),e=monthAt(x.startMonth,x.duration-1);return n<x.startMonth?['قريباً','upcoming']:n>e?['منتهية','ended']:['فعّالة','active']}function esc(s=''){return String(s).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}
function render(){monthPill.textContent=pretty(thisMonth());const act=items.filter(x=>statusOf(x)[1]==='active'),now=thisMonth();due.textContent=money.format(act.filter(x=>!x.paid.includes(now)).reduce((a,x)=>a+x.monthly,0));activeCount.textContent=act.length;paidNow.textContent=`${money.format(act.filter(x=>x.paid.includes(now)).reduce((a,x)=>a+x.monthly,0))} د.أ`;countBadge.hidden=!items.length;countBadge.textContent=`${items.length} جمعية`;if(!items.length){content.innerHTML=`<div class="empty-state"><div class="empty-icon">✓</div><h3>لسه ما أضفت أي جمعية</h3><p>أضف أول جمعية، وإحنا بنرتّبلك كل شهر وموعد استلامك.</p><button class="primary-button" id="emptyAdd">أضف أول جمعية</button></div>`;emptyAdd.onclick=openModal;return}content.innerHTML=`<div class="association-grid">${items.map(card).join('')}</div>`;content.querySelectorAll('[data-del]').forEach(b=>b.onclick=()=>removeItem(b.dataset.del));content.querySelectorAll('[data-open]').forEach(b=>b.onclick=()=>{expanded=expanded===b.dataset.open?null:b.dataset.open;render()});content.querySelectorAll('[data-paid]').forEach(c=>c.onchange=()=>togglePaid(c.dataset.id,c.dataset.month))}
function card(x){const ms=Array.from({length:x.duration},(_,i)=>monthAt(x.startMonth,i)),rec=monthAt(x.startMonth,x.turn-1),st=statusOf(x),pc=x.paid.filter(m=>ms.includes(m)).length,open=expanded===x.id;return `<article class="association-card"><div class="card-main"><div class="card-title-row"><div class="association-symbol">${esc(x.name.charAt(0))}</div><div class="card-title"><div><h3>${esc(x.name)}</h3><span class="status ${st[1]}">${st[0]}</span></div><p>${pretty(x.startMonth)} — ${pretty(ms.at(-1))}</p></div><button class="delete-button" data-del="${x.id}">حذف</button></div><div class="money-row"><div><span>الدفعة الشهرية</span><strong>${money.format(x.monthly)} <small>د.أ</small></strong></div><div><span>قيمة الجمعية</span><strong>${money.format(x.total)} <small>د.أ</small></strong></div></div><div class="receive-banner"><span class="receive-dot"></span><div><small>موعد استلامك · الدور ${x.turn}</small><strong>${pretty(rec)}</strong></div></div><div class="progress-block"><div class="progress-label"><span>الدفعات المكتملة</span><b>${pc} من ${x.duration}</b></div><div class="progress-track"><span style="width:${pc/x.duration*100}%"></span></div></div><button class="months-toggle" data-open="${x.id}">${open?'إخفاء جدول الشهور':'عرض جدول الشهور'}<span>${open?'⌃':'⌄'}</span></button></div>${open?`<div class="months-list">${ms.map((m,i)=>monthRow(x,m,i)).join('')}</div>`:''}</article>`}
function monthRow(x,m,i){const p=x.paid.includes(m),t=i+1===x.turn,c=m===thisMonth();return `<label class="month-row ${p?'paid':''} ${t?'turn':''}"><input type="checkbox" ${p?'checked':''} data-paid data-id="${x.id}" data-month="${m}"><span class="checkmark">✓</span><div class="month-name"><strong>${pretty(m)}</strong><small>${t?'موعد استلامك':c?'هذا الشهر':`الدفعة ${i+1}`}</small></div><b>${money.format(x.monthly)} د.أ</b></label>`}function togglePaid(id,m){items=items.map(x=>x.id!==id?x:{...x,paid:x.paid.includes(m)?x.paid.filter(v=>v!==m):[...x.paid,m]});save();render()}function removeItem(id){const x=items.find(v=>v.id===id);if(!x||!confirm(`متأكد إنك بدك تحذف جمعية «${x.name}»؟`))return;items=items.filter(v=>v.id!==id);save();render()}
function openModal(){modal.hidden=false;fStart.value=thisMonth();updatePreview();setTimeout(()=>fName.focus(),20)}function closeModal(){modal.hidden=true;form.reset();fDuration.value=12;fTurn.value=1}function updatePreview(){const d=Number(fDuration.value)||1,t=Math.min(d,Math.max(1,Number(fTurn.value)||1));preview.textContent=fStart.value?pretty(monthAt(fStart.value,t-1)):'—'}addBtn.onclick=openModal;closeBtn.onclick=closeModal;modal.onclick=e=>{if(e.target===modal)closeModal()};[fStart,fDuration,fTurn].forEach(x=>x.oninput=updatePreview);form.onsubmit=e=>{e.preventDefault();const duration=Math.max(1,Number(fDuration.value)),monthly=Math.max(0,Number(fMonthly.value)),turn=Math.min(duration,Math.max(1,Number(fTurn.value))),total=fTotal.value?Math.max(0,Number(fTotal.value)):monthly*duration;if(!fName.value.trim()||!fStart.value||monthly<=0)return;const x={id:crypto.randomUUID?.()||`${Date.now()}-${Math.random()}`,name:fName.value.trim(),startMonth:fStart.value,duration,turn,total,monthly,paid:[]};items=[x,...items];expanded=x.id;save();closeModal();render()};render();